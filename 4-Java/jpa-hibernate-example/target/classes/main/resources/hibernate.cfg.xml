<?xml version="1.0" encoding="UTF-8"?>
<hibernate-configuration>

    <session-factory>
        <!-- Driver e URL de Conexão com o PostgreSQL -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/cafeteria</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">ROOT</property>

        <!-- Dialeto do PostgreSQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Habilitar o Hibernate para gerar o SQL -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Gerenciamento da criação de tabelas (criar/atualizar) -->
        <property name="hibernate.hbm2ddl.auto">update</property>
    </session-factory>

</hibernate-configuration>
